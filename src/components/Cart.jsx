import React from 'react';
import { getStoredCart } from '../utils/fakedb';

const Cart = () => {
    const cart = getStoredCart();
    
    return (
        <div>
            
        </div>
    );
};

export default Cart;